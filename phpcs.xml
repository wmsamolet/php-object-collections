<?xml version="1.0"?>
<ruleset name="WMSamolet Coding Standard">
    <!-- display progress -->
    <arg value="p"/>

    <!-- use colors in output -->
    <arg name="colors"/>

    <!-- use utf-8 encoding -->
    <arg name="encoding" value="utf-8"/>

    <!-- inherit rules from: -->
    <rule ref="PSR12"/>

    <file>src</file>
</ruleset>